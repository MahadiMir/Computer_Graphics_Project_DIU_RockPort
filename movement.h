#ifndef MOVEMENT_H_INCLUDED
#define MOVEMENT_H_INCLUDED
/*
//movement
static float	fx	=  0.0;//cloud
int serial=0;

static void Timer(int value)
{
    serial += 1;

    glutPostRedisplay();
    // 100 milliseconds
    glutTimerFunc(1000, Timer, 0);
}

void bird_fly(){
tx=tx-1;
    if(tx<-130)
        tx = 50;
        glutPostRedisplay();
}

void cloud_move(){
    //if(cld>=0|| cld<0)
    cld=cld+1;
    if(cld>50)
        cld = -70;

    //if(cld1>=0|| cld1<0)
    cld1=cld1+1;
    if(cld1>50)
        cld1 = -70;

    //if(cld2>=0|| cld2<0)
    cld2=cld2+.8;
    if(cld2>50)
        cld2 = -70;

   // if(cld3>=0|| cld3<0)
    cld3=cld3+.9;
    if(cld3>50)
        cld3 = -70;

        glutPostRedisplay();
}


//movement ends

*/

#endif // MOVEMENT_H_INCLUDED
